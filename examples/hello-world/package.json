{
  "name": "hello-world",
  "version": "1.0.3",
  "description": "**Serverless framework** plugin for blazingly-fast rust functions.",
  "scripts": {
    "deploy": "serverless deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "serverless-plugin-rust": "file:../.."
  }
}
